node_modules

.env
.env.local
.DS_Store
.cache
.output
.nitro
.tanstack

/dist/
/dist-ssr/

# Vercel Build Output API bundle (produced by build-vercel.sh at go-live).
/.vercel/

# Runtime server log written on start.
/.run/

# Generated by the TanStack Start router plugin on dev/build.
src/routeTree.gen.ts
.vercel

# Unreferenced local assets (notesnap-profile.png) — not part of the site build.
/assets/
.env*

# Large one-off build artifacts (hosted for downloads, never committed)
public/play-assets/*.aab
dist/**

# Native fpcalc binary provisioned by publish.sh (downloaded from GitHub releases)
src/services/fpcalc
